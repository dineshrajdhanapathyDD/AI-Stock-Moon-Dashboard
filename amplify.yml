version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Preparing static landing page"
        - ls -la
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - README.md