# 🤝 Contributing

## Quick Start

1. **Fork & Clone**
   ```bash
   git clone https://github.com/your-username/stock-moon-dashboard.git
   cd stock-moon-dashboard
   ```

2. **Setup Environment**
   ```bash
   pip install -r requirements.txt
   python app.py  # Test locally
   ```

3. **Run Tests**
   ```bash
   python test_complete_system.py
   ```

## What We Need

- 🐛 **Bug fixes** - Check issues tab
- 📊 **New stock markets** - Add to `src/stock_database.py`
- 📈 **Visualization improvements** - Enhance `src/visualizations.py`
- 🧪 **Test coverage** - Add tests for edge cases
- 📚 **Documentation** - Improve guides and examples

## Submission Process

1. Create feature branch: `git checkout -b feature/your-feature`
2. Make changes and test: `python build.py`
3. Commit: `git commit -m "Add: your feature description"`
4. Push: `git push origin feature/your-feature`
5. Open Pull Request with description

## Code Style

- Follow existing patterns in the codebase
- Add type hints for new functions
- Include docstrings for public methods
- Run `python build.py` before submitting

## Questions?

Open an issue or check existing discussions. We're here to help! 🚀